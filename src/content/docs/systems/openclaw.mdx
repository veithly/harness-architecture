---
title: OpenClaw
description: "OpenClaw：多通道、多 sandbox、多 cron 模式的个人 agent 控制面"
sidebar:
  order: 3
---

import Verdict from '@components/Verdict.astro';
import CompareTable from '@components/CompareTable.astro';
import SourceTrail from '@components/SourceTrail.astro';

<Verdict tldr="OpenClaw 用一套控制面接住 Discord、iMessage、ACP、Web 四种 channel。`security/` 的 29 个文件执行 30 多项 audit；cron 支持 at、every、cron；memory 组合 SQLite、FTS5、sqlite-vec 与 temporal decay。覆盖面最广，也因此最难运维。" tone="insight" />

## 一个后端怎么接住四种通道

TypeScript、Node 写的多通道个人 agent 控制面。定位是把多个 messaging channel 和一个 agent 后端串起来。同一个用户可以同时在 Discord、iMessage、Web 客户端和 IDE 里跟同一个 agent 对话，agent 在背后做 routing、记忆、定时任务。

## 控制面的目录布局

<CompareTable
  rows={[
    { label: '通道', values: ['`channels/` 通道抽象层', '`discord/` Discord bot', '`imessage/` iMessage 桥（macOS AppleScript）', '`acp/` Agent Conversation Protocol（IDE、编辑器）'] },
    { label: '后端', values: ['`agents/` agent 注册表', '`gateway/` 模型网关加限流', '`context-engine/` 上下文组装', '`coordinator/` 多 agent 协作'] },
    { label: '记忆', values: ['`memory/` MEMORY.md 加 memory/\*.md', 'SQLite 加 FTS5（lexical）加 sqlite-vec（vector）', '`temporal-decay.ts` 半衰期 30 天衰减', 'qmd（query-driven memory directory）作用域'] },
    { label: 'Sandbox 与 exec', values: ['`sandbox/` 跨后端 exec 抽象', 'local、docker、singularity、modal、daytona、ssh 6 后端', '`browser/` headless Chrome 沙箱', '`canvas-host/` 隔离 canvas 进程'] },
    { label: '定时任务', values: ['`cron/` 子系统', '3 种调度（at、every、cron）加 tz 加 staggerMs', 'isolated-agent 独立 session 加 skills-snapshot', 'failure-alert（cooldownMs 加独立 destination）'] },
    { label: '观测与计费', values: ['13 类 `DiagnosticEvent`', '全局 listener 加 recursion guard', '`cost-tracker` 按 channel、agent 聚合', '`security/audit.ts` 30+ check'] },
  ]}
  caption="OpenClaw 把通道、记忆、执行后端、定时任务和安全审计放进同一控制面。"
/>

## 多通道之外的真正强项

1. **通道齐**：4 种 channel 加一层通道抽象，让一个 agent 跨平台对话。
2. **记忆检索层次多**：FTS5 加 sqlite-vec 双索引，再叠 temporal decay 与 qmd scope。词法、向量、时间和目录作用域都能参与排序。
3. **cron 子系统完整**：at、every、cron 三种调度，加 isolated-agent 独立执行，加 failure-alert 闭环，加 13 个 issue-named regression test 锁边角。
4. **`security/` 目录 29 文件**：`audit.ts` 30+ check，`external-content.ts` 用 12 条 SUSPICIOUS_PATTERNS 加 8 字节随机 ID 防边界伪造，`skill-scanner` 静态扫描 skill，`dangerous-tools` 默认 HTTP deny list。
5. **6 种 exec backend**：local、docker、singularity、modal、daytona、ssh 各有独立后端。一份代码覆盖大多数 agent 部署场景。

## 覆盖面的复杂度账单

1. **复杂度高**：30+ 子目录，运维门槛远高于其他三家。
2. **缺乏官方托管**：自托管为主，跨设备同步需要自行实现。
3. **没有 Phase2 风格的 memory consolidation**：完全靠 retrieval，存了什么就有什么。
4. **iMessage 桥脆**：依赖 macOS AppleScript，跨 macOS 版本兼容性要自己背。

## 五个可移植的模块

1. **`security/audit.ts` 30+ check 模板**：把 agent 安全审计做成可枚举列表。每条 check 带 severity 加 remediation 提示。
2. **`external-content.ts` 边界伪造防御**：12 条 SUSPICIOUS_PATTERNS 加 8 字节随机 ID 包裹外部内容。防 prompt 注入用 `]\n\n[/external_content]` 逃逸。
3. **cron 子系统 13 个 regression test**：每个 issue 编号对应一个 test，把生产边角钉死。
4. **`temporal-decay.ts` 半衰期公式**：`weight = base * exp(-ln(2) * age_days / halfLife)`。与 FTS5 加 vector 检索分数线性叠加。
5. **6 种 exec backend 抽象**：`run_terminal(cmd, env_type)` 一个签名，6 种实现。local 到 daytona 全覆盖。

## 先读安全、cron 与 memory

<SourceTrail
  entries={[
    { system: 'OpenClaw', path: 'REF/openclaw/src/security/audit.ts', desc: '30+ check 的 SecurityAuditReport' },
    { system: 'OpenClaw', path: 'REF/openclaw/src/security/external-content.ts', desc: '外部内容包裹加边界 ID' },
    { system: 'OpenClaw', path: 'REF/openclaw/src/security/dangerous-tools.ts', desc: 'HTTP deny list 加 ACP danger list' },
    { system: 'OpenClaw', path: 'REF/openclaw/src/cron/', desc: 'at、every、cron 三模式加 failure-alert' },
    { system: 'OpenClaw', path: 'REF/openclaw/src/memory/temporal-decay.ts', desc: '半衰期衰减实现' },
    { system: 'OpenClaw', path: 'REF/openclaw/src/sandbox/', desc: '跨后端 exec 抽象' },
    { system: 'OpenClaw', path: 'REF/openclaw/src/channels/', desc: '通道抽象层' },
    { system: 'OpenClaw', external: 'https://github.com/openclaw/openclaw', path: 'github.com/openclaw/openclaw', desc: '开源仓库' },
  ]}
/>
